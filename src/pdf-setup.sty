% ===========================
% 📄 PDF И ТИПОГРАФИКА (улучшенная версия)
% ===========================
% Только компоненты, специфичные для PDF и тонкой типографики

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{pdf-setup}[2025/10/25 PDF and typography enhancements]

% --- Поддержка копирования кириллицы в PDF ---
\IfFileExists{cmap.sty}{
  \RequirePackage{cmap}
}{}

% Оставить здесь или в layoutsetup.sty
% --- Улучшение переносов и структуры текста ---
\IfFileExists{impnattypo.sty}{
  \ifluatex
  \RequirePackage[hyphenation, lastparline, nosingleletter]{impnattypo}
  \else
  \RequirePackage[hyphenation, lastparline]{impnattypo}
  \fi
}{}

% 📌 Примечания:
% - cmap: позволяет корректно выделять и копировать русский текст в PDF.
% - impnattypo: улучшает читаемость (избегает одиночных букв в конце строк и т.д.).
% - Этот модуль опционален — если файлы отсутствуют, ничего не ломается.

% Этот блок закоментирован и перенесен из graphicssetup.sty что бы знать где искать
% \RequirePackage{rotating}      % Для sidewaystable
% \RequirePackage{pdflscape}     % Для \begin{landscape}
% \RequirePackage{pdfpages}      % Для вставки PDF
% Для альбомных таблиц:
%   \usepackage{rotating}      % → \begin{sidewaystable}...\end{sidewaystable}
%   \usepackage{longtable}     % → таблицы на несколько страниц
\endinput
