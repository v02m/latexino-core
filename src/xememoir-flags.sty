% ======================================================================
% 🧩  ОБЩИЕ ФЛАГИ ПАКЕТА xememoir
% ======================================================================
% NOTE: Пока ручные флаги, позже можно перейти на kvoptions
% Этот модуль задаёт системные флаги для всех компонентов xememoir.
% Используется как точка централизованного управления стилями.
% ======================================================================

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{xememoir-flags}[2025/11/02 Core flags module for XeMemoir]

\makeatletter

% ======================================================================
% 🎛️  ВЫБОР РЕЖИМА: СТАРЫЙ/НОВЫЙ
% ======================================================================
% \xememoir@usenewtrue   % переключатель на новую модульную структуру
% \xememoir@usenewfalse  % если нужно оставить старое поведение

\newif\ifxememoir@usenew
\xememoir@usenewtrue

% ======================================================================
% ⚙️  ФЛАГИ ДЛЯ  ШРИФТОВ
% ======================================================================

% Флаги выбора семейств шрифтов
\newif\iffontfamily@ms          % Microsoft (Times New Roman, Arial)
\newif\iffontfamily@liberation  % Liberation (Linux-совместимые аналоги)
\newif\iffontfamily@cmu         % Computer Modern Unicode (классика)
\newif\iffontfamily@pscyr       % Пакет pscyr (русские шрифты CM)
\newif\iffontfamily@texgyre     % Tex Gyre (современные open fonts)
\newif\iffontfamily@alt         % Альтернативный набор (fallback)
\newif\iffontfamily@system      % Системные шрифты по умолчанию

% ✅ Установка профиля по умолчанию
% (при необходимости — переопределяется через опции пакета)
%\fontfamily@cmutrue
\fontfamily@mstrue

% ======================================================================
% 📖  ТИПОГРАФИЧЕСКИЕ ПРОФИЛИ КНИГИ
% ======================================================================
\newif\ifbookstyle@classic    % Классический стиль (традиционный макет)
\newif\ifbookstyle@modern     % Современный стиль (минимализм, чистые линии)
\newif\ifbookstyle@academic   % Академический стиль (для научных текстов)
\newif\ifbookstyle@poetry     % Поэтич. стиль (увелич. поля, свободн. формат)
\newif\ifbookstyle@technical  % Технический (моноширинные элементы, сетка)
\newif\ifbookstyle@elegant    % Элегантный (декоративные шрифты, баланс)
\newif\ifbookstyle@compact    % Компактный (уменьшенные интервалы, поля)
\newif\ifbookstyle@bold       % Акцентный (усиленные заголовки, контраст)

% Профиль по умолчанию
\bookstyle@classictrue

% ======================================================================
% 🎨  ГРАФИЧЕСКИЕ ПРОФИЛИ
% ======================================================================
\newif\ifgraphicstyle@classic      % Классический (ч/б, без эффектов)
\newif\ifgraphicstyle@colorful     % Цветной, контрастный
\newif\ifgraphicstyle@minimal      % Минималистичный
\newif\ifgraphicstyle@technical    % Технический (сетка, подписи)
\newif\ifgraphicstyle@presentation % Презентационный (яркий, крупный текст)

% Профиль по умолчанию
\graphicstyle@classictrue

% ======================================================================
% 🔢  СТИЛИ СПИСКОВ
% ======================================================================
\newif\ifliststyle@gost       % ГОСТ — формат по российскому стандарту
\newif\ifliststyle@numeric    % Нумерованный список (1, 2, 3...)
\newif\ifliststyle@mixed      % Смешанный стиль (буквы + цифры)
\newif\ifliststyle@alpha      % Буквенный стиль (a, b, c...)

% Профиль по умолчанию
\liststyle@gosttrue

% ======================================================================
% ⚙️  Значения по умолчанию (можно закомментировать для теста)
% ======================================================================
%\fontfamily@mstrue       % по умолчанию используем Microsoft fonts
%\bookstyle@classictrue % типографика — классическая
%\graphicstyle@classictrue % графика — классическая
%\liststyle@gosttrue    % списки — ГОСТ

% ======================================================================
% 🔹  ПРИМЕЧАНИЯ
% ======================================================================
% Если при тестировании закомментировать все флаги, включится fallback:
%   - Шрифты CMU
%   - Типографика classic
%   - Графика classic
%   - Списки ГОСТ

\makeatother
\endinput